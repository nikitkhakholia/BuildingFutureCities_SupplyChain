const fs = require("fs")
const { network } = require("../../constants/cAddresses.json")

const frontEndContractsFile = "../../constants/contractAddresses.json"
const frontEndAbiFile = "../../constants/abi.json"
